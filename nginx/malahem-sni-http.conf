# Dedicated HTTP vhost for malahem.sarhsa.online.
# Appended to the live Sarh edge. Does not change sarhsa.online.
server {
  listen 80;
  server_name malahem.sarhsa.online;
  client_max_body_size 32m;

  include /etc/nginx/includes/malahem-sni-locations.conf;
}
